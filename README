Kernel README
=============

This is a kernel for the i536 architecture.

WTF is i586? Learn here:
  http://myonlineusb.wordpress.com/2011/06/08/what-is-the-difference-between-i386-i486-i586-i686-i786/

Build
-----

The kernel is build using gcc and binutils, and being compiled for the
i536 architecture. A good tutorial for building a cross compiler that
will build this kernel is on osdev: http://wiki.osdev.org/GCC_Cross-Compiler.

You will also need to install the GRUB utility programs and possibly xorisso.

Bochs
-----

I am using bochs to test and run the kernel. You can download bochs from the
bochs sourceforge page: https://sourceforge.net/projects/bochs/files/bochs/.

Make sure you compile bochs with the debugging enabled:

   ./configure \
            --enable-pci \
            --enable-vmx \
            --enable-debugger \
            --enable-disasm \
            --enable-debugger-gui \
            --enable-logging \
            --enable-cdrom \
            --enable-fpu \
            --enable-x86-debugger \
            --enable-iodebug \
            --disable-plugins \
            --disable-docbook \
            --with-x --with-x11 --with-term

README TODO
-----------
    * name project
    * describe build system
    * how to use bochs
    * how bochs debugging is implemented
    * how to use qemu
